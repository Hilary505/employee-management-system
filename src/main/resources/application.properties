spring.application.name=employee-management-system

# ==========================================
# H2 In-Memory Database
# ==========================================
spring.datasource.url=jdbc:h2:mem:employeedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# ==========================================
# JPA / Hibernate
# ==========================================
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# update: keeps existing data, auto-creates/alters tables on startup
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ==========================================
# H2 Console — accessible at /h2-console
# ==========================================
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ==========================================
# MySQL (uncomment to switch from H2)
# ==========================================
# spring.datasource.url=jdbc:mysql://localhost:3306/employeedb?useSSL=false&serverTimezone=UTC
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=<db_username>
# spring.datasource.password=<db_password>
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ==========================================
# PostgreSQL (uncomment to switch from H2)
# ==========================================
# spring.datasource.url=jdbc:postgresql://localhost:5432/employeedb
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=<db_username>
# spring.datasource.password=<db_password>
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
